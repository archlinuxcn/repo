maintainers:
  - github: Integral-Tech
    email: integral@murena.io

pre_build_script: |
  aur_pre_build(maintainers=['z3ntu'])
  add_arch(['aarch64','armv7h'])

post_build: aur_post_build

repo_depends:
  - dotnet-core-bin

update_on:
  - source: aur
    aur: jellyfin

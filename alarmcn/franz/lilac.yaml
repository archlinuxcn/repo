#

maintainers:
  - github: yuyichao

post_build: git_pkgbuild_commit

# For nvm
build_prefix: archlinuxcn-aarch64

repo_depends:
  - electron22-bin

update_on:
  - source: manual
    manual: 5.10.0
  - source: manual
    manual: 1

maintainers:
  - github: xiaohuirong
    email: horeamxiao@archlinuxcn.com

pre_build_script: |
  aur_pre_build()
  add_makedepends(['git'])
  add_depends(['libevdev'])
post_build: aur_post_build

update_on:
  - source: aur
    aur: syshud

maintainers:
  - github: yuyichao

pre_build: vcs_update
post_build: git_pkgbuild_commit

repo_depends:
  - llvm-julia
  - llvm-julia: llvm-libs-julia

update_on:
  - source: vcs

update_on_build:
  - pkgbase: llvm-julia

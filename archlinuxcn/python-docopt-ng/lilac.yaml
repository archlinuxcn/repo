build_prefix: extra-x86_64
pre_build_script: |
  aur_pre_build(maintainers=['Grafcube'])
  add_makedepends(['git'])
post_build: aur_post_build
update_on:
  - source: aur
    aur: python-docopt-ng
  - alias: python

maintainers:
  - github: roaldclark

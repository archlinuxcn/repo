maintainers:
  - github: axionl

build_prefix: extra-x86_64

pre_build: aur_pre_build

pre_build_script: aur_pre_build(maintainers=['DuckSoft', 'yochananmarqos'])

post_build: aur_post_build

update_on:
  - source: aur
    aur: apostrophe
  - alias: python

maintainers:
  - github: heavysink
    email: winstonwu91@gmail.com

build_prefix: extra-x86_64

pre_build: vcs_update

post_build: git_pkgbuild_commit

update_on:
  # The upstream Git host is unreachable; follow AUR recipe updates for now.
  - source: aur
    aur: ryujinx-git

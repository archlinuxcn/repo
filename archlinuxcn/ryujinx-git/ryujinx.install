pre_install() {
	if [ -d "/opt/ryujinx/Logs" ];
	then
		rm -rf /opt/ryujinx/Logs
	fi
}

pre_upgrade() {
	if [ -d "/opt/ryujinx/Logs" ];
	then
		rm -rf /opt/ryujinx/Logs
	fi
}

pre_remove() {
	if [ -d "/opt/ryujinx/Logs" ];
	then
		rm -rf /opt/ryujinx/Logs
	fi
}

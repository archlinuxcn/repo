maintainers:
  - github: Kimiblock

pre_build_script: vcs_update()
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()

update_on:
  - source: "git"
    git: "https://github.com/XTLS/RealiTLScanner"
    use_commit: true
build_prefix: archlinuxcn-x86_64
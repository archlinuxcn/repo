# This file is auto-generated by n2y

maintainers:
  - github: ykelvis
  - github: DDoSolitary

build_prefix: extra-x86_64

update_on:
  - source: aur
    aur: libpdfium-nojs
  - source: cmd
    cmd: |
      bash -eo pipefail -c '
        ver=$(curl -fsSL https://omahaproxy.appspot.com/linux?channel=stable | cut -d. -f3)
        rev=$(curl -fsSL https://pdfium.googlesource.com/pdfium/+log/master..chromium/$ver?format=json | (grep -c "\"commit\":" || true))
        echo $ver.r$rev'
  - alias: icu

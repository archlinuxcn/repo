maintainers:
  - github: taotieren

build_prefix: extra-x86_64

repo_depends:
  - python-defcon
  - python-fontmath

post_build_script: |
  git_pkgbuild_commit()

update_on:
  - source: pypi
    pypi: MutatorMath
  - alias: python

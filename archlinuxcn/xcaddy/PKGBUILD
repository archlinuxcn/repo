# Maintainer: OpenSorcerer <alex cat fossfed dog eu>
pkgname=xcaddy
pkgver=0.4.7
pkgrel=1
pkgdesc="Build Caddy with plugins"
arch=('i686' 'x86_64' 'armv7h' 'aarch64')
url="https://github.com/caddyserver/xcaddy"
license=('Apache-2.0')
depends=('go' 'glibc')
makedepends=('git')
source=("git+$url.git#tag=v$pkgver")
sha512sums=('b89316c04676e650e8b861d6322d396303da09e663fd98432db138c4c9ead4a6d58b5dbff2fdb103587656d86f818c64345fcd792155d250e677eae91c14424d')

build() {
    cd "$pkgname"
    export CGO_CPPFLAGS="${CPPFLAGS}"
    export CGO_CFLAGS="${CFLAGS}"
    export CGO_CXXFLAGS="${CXXFLAGS}"
    export CGO_LDFLAGS="${LDFLAGS}"
    export GOFLAGS="-buildmode=pie -trimpath -modcacherw"
    go build -o xcaddy "cmd/$pkgname/main.go"
}

package() {
    cd "$pkgname"
    install -Dm755 "./xcaddy" "$pkgdir/usr/bin/xcaddy"
}

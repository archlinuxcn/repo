maintainers:
  - github: axionl

build_prefix: extra-x86_64

pre_build: aur_pre_build

post_build: aur_pre_build(maintainers=['sperg512', 'ckatri', 'Icelk'])

update_on:
  - source: aur
    aur: vscodium-bin

maintainers:
  - github: axionl

build_prefix: extra-x86_64

pre_build: aur_pre_build(maintainers=['sperg512', 'ckatri', 'Icelk'])

post_build: aur_post_build

update_on:
  - source: aur
    aur: vscodium-bin

maintainers:
  - github: poscat0x04

build_prefix: archlinuxcn-x86_64

pre_build_script: |
  aur_pre_build(maintainers=['fryfrog'])
post_build_script: |
  aur_post_build()
repo_depends:
  - perl-config-inifiles

update_on:
  - source: aur
    aur: sanoid

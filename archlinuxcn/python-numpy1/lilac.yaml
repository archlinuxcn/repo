maintainers:
  - github: Kimiblock

#pre_build_script: update_pkgver_and_pkgrel(_G.newver)
pre_build_script: |
  update_pkgrel()

repo_depends:
  - cython0

update_on:
  - alias: "python"
  - source: "aur"
    aur: "python-numpy1"
build_prefix: archlinuxcn-x86_64
maintainers:
  - github: kiri2002
    email: kiri@vern.cc

build_prefix: extra-x86_64

pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver.lstrip('v'))

post_build_script: |
  git_pkgbuild_commit()

update_on:
  - source: github
    github: yifeikong/curl_cffi 
    use_latest_tag: true

maintainers:
  - github: ZeekoZhu

build_prefix: extra-x86_64

pre_build_script: |
  aur_pre_build(mantainers='Zeeko Zhu')

post_build_script: |
  aur_post_build()
update_on:
  - source: github
    github: kmonad/kmonad

maintainers:
  - github: Integral-Tech
    email: integral@archlinuxcn.org

post_build_script: |
  aur_post_build()
  git_pkgbuild_commit()

update_on:
  - source: aur
    aur: flutter

  - source: github
    github: flutter/flutter
    use_max_tag: true
    exclude_regex: ".*pre"

  - source: alpm
    alpm: dart

#!/usr/bin/env python

from setuptools import find_packages, setup

setup(
    name = "seafevents",
    version = "@SEAFEVENTS_VERSION@",
    author = "Seafile Ltd.",
    author_email = "info@seafile.com",
    description = ("Seafile backend tasks daemon"),
    license = "Apache2",
    url = "https://github.com/haiwen/seafevents",
    packages = find_packages(exclude=["test.*", "test"])
)

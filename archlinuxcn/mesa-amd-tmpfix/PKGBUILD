pkgbase=mesa-amd-tmpfix
pkgname=(
  libva-mesa-driver-amd-tmpfix
)

pkgver=24.1.5.2
pkgrel=1
epoch=1
pkgdesc="You should uninstall this package and use libva-mesa-driver instead."
url="https://www.mesa3d.org/"
arch=(x86_64)
license=("None")
install='warning.install'

package_libva-mesa-driver-amd-tmpfix() {
  depends=(
    'libva-mesa-driver>=1:24.1.5-2'
  )
}

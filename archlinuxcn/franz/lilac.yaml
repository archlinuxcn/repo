#

maintainers:
  - github: yuyichao

post_build: git_pkgbuild_commit

repo_depends:
  - nvm

update_on:
  - source: manual
    manual: 5.10.0
  - source: alpm
    from_pattern: ^(\d+\.\d+.\d+).*
    to_pattern: \1
    alpm: electron22
  - source: manual
    manual: 1

#

maintainers:
  - github: yuyichao

build_prefix: extra-x86_64

# pre_build: aur_pre_build
# post_build: aur_post_build

update_on:
  - source: manual
    manual: 5.5.0
  - source: archpkg
    archpkg: electron
    from_pattern: ^(\d+\.\d+)\..*
    to_pattern: \1
  - source: manual
    manual: 6

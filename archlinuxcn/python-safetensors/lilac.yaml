maintainers:
  - github: hubutui

build_prefix: extra-x86_64

update_on:
  - source: github
    github: huggingface/safetensors
    use_max_tag: true
    include_regex: v(\d+.\d+.\d+)
  - alias: python
# vim:set ts=2 sw=2 et:

maintainers:
  - github: ykelvis

build_prefix: extra-x86_64

post_build: aur_post_build

repo_depends:
  - libgccjit

update_on:
  - source: git
    git: git://git.savannah.gnu.org/emacs.git
  - source: aur
    aur: emacs-git

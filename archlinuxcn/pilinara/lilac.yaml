maintainers:
  - github: nlsdt
    email: nlsdt <nlsdt@archlinuxcn.org>

pre_build_script: update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()

update_on:
  - source: github
    github: Starfallan/PiliNara
    use_latest_release: true
    exclude_regex: '.*pre.*'

maintainers:
  - github: axionl

build_prefix: extra-x86_64

pre_build_script: aur_pre_build(maintainers=['feuri', 'frealgagu'])

post_build: aur_post_build

update_on:
  - source: aur
    aur: python-pysdl2
  - alias: python

build_prefix: extra-x86_64
maintainers:
  - github: hubutui
repo_depends:
  - r-iterators
update_on:
  - regex: foreach_([\d._-]+).tar.gz
    source: regex
    url: https://cran.r-project.org/package=foreach

maintainers:
  - github: Sasasu

build_prefix: archlinuxcn-x86_64

pre_build_script: aur_pre_build(maintainers=['a821'])

post_build: aur_post_build

repo_depends: [ python-pgspecial ]

update_on:
  - source: aur
    aur: pgcli
  - alias: python

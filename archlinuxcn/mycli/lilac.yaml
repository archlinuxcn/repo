maintainers:
  - github: Sasasu

build_prefix: extra-x86_64

pre_build_script: aur_pre_build(maintainers=['loonies'])

post_build: aur_post_build

repo_depends:
  - python-sqlglot

update_on:
  - source: aur
    aur: mycli
  - alias: python

# This file is auto-generated by n2y

maintainers:
  - github: heavysink
    email: winstonwu91@gmail.com

build_prefix: extra-x86_64

pre_build_script: |
  aur_pre_build(maintainers=['meadow'])
  for line in edit_file('PKGBUILD'):
    print(line)
    if 'test_mcast "mcast-join-loop"' in line:
      print('')
      print('\t# OCaml 5.5 exposes get_sockaddr as a compatibility macro. Lwt\'s redundant')
      print('\t# declaration collides with that macro; socketaddr.h is sufficient.')
      print("\tsed -i '/^extern void get_sockaddr(/,+1d' src/unix/unix_c/unix_recv_send_utils.h")

post_build: aur_post_build

repo_depends:
  - ocaml-ocplib-endian
  - ocaml-ppxlib
  - ocaml-react

repo_makedepends:
  - cppo
  - ocaml-ppx_let

update_on:
  - source: aur
    aur: 

maintainers:
  - github: hubutui

build_prefix: extra-x86_64

pre_build_script: aur_pre_build(maintainers=['hottea'])

post_build: aur_post_build

update_on:
  - source: aur
    aur: 3dmeshmetric-bin

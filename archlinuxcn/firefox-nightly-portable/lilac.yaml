maintainers:
  - github: Kimiblock

#pre_build_script: update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()
#  update_aur_repo()

update_on:
  - source: cmd
    cmd: bash -c "date -I | sed 's|-|_|g'"

repo_depends:
  - portable
  #- firefox-nightly

build_prefix: archlinuxcn-x86_64
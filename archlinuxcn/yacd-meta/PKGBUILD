# Maintainer: Rocket Aaron <i at rocka dot me>
# Contributor: Dct Mei <dctxmei@yandex.com>

pkgname=yacd-meta
_pkgname=Yacd-meta
pkgver=0.4.0
pkgrel=1
pkgdesc="Yet Another Clash Dashboard"
arch=('any')
url="https://github.com/MetaCubeX/Yacd-meta"
license=('MIT')
makedepends=('bun')
source=("${url}/archive/v${pkgver}.tar.gz")
sha256sums=('14d667a500d83d47d7f129e1140ab15ec17ee8785fb99c8f03ac5b30a19ca945')

build() {
    cd "${_pkgname}-${pkgver}"
    bun install --frozen-lockfile
    bun run build
}

package() {
    mkdir -p "${pkgdir}/usr/share"
    cp -r "${_pkgname}-${pkgver}/public" "${pkgdir}/usr/share/${pkgname}"
}

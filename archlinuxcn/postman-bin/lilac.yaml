maintainers:
  - github: SilverRainZ
    email: Shengyu Zhang <la@archlinuxcn.org>

build_prefix: extra-x86_64

pre_build_script: aur_pre_build(maintainers=['j.taala'])

post_build: aur_post_build

update_on:
  - source: aur
    aur: postman-bin

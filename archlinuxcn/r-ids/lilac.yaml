build_prefix: extra-x86_64
maintainers:
  - github: hubutui
repo_depends:
  - r-openssl
  - r-uuid
update_on:
  - regex: ids_([\d._-]+).tar.gz
    source: regex
    url: https://cran.r-project.org/package=ids

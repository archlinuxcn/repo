maintainers:
  - github: MarvelousBlack

build_prefix: multilib

pre_build_script: aur_pre_build(maintainers=['ThePooN'])

post_build: aur_post_build

time_limit_hours: 4

update_on:
  - source: aur
    aur: wine-osu

pre_build: aur_pre_build

post_build: aur_post_build

update_on:
  - source: aur
    aur: ocaml-menhir
  - source: archpkg
    archpkg: ocaml
    from_pattern: '^(\d+\.\d+)\..*'
    to_pattern: '\1'

maintainers:
  - github: dctxmei

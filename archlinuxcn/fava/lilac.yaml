maintainers:
  - github: lilydjwg

pre_build_script: |
  aur_pre_build(maintainers=['wzyboy'])

post_build: aur_post_build

repo_depends:
  - beancount
  - beanquery
  - beangulp

update_on:
  - source: aur
    aur: fava
  - alias: python

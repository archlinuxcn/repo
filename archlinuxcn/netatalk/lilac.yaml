maintainers:
  - github: ykelvis

build_prefix: extra-x86_64

pre_build_script: aur_pre_build(maintainers=['denn'])

post_build: aur_post_build

repo_depends:
  - bstring

update_on:
  - source: aur
    aur: netatalk
  - alias: libcrypto
  - alias: libssl

maintainers:
  - github: Yurzi
    email: yurzi@foxmail.com

build_prefix: extra-x86_64

pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)

post_build_script: |
  git_pkgbuild_commit()

update_on:
  - source: regex
    url: https://www.lre.epita.fr/dload/spot/
    regex: spot-(\d+\.\d+\.\d+)\.tar\.gz

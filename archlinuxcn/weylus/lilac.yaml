maintainers:
  - github: Kimiblock

pre_build_script: |
  aur_pre_build
  vcs_update
post_build_script: |
  git_pkgbuild_commit()
#  update_aur_repo()

update_on:
  - source: "git"
    git: "https://github.com/H-M-H/Weylus.git"
    prefix: v
build_prefix: extra-x86_64

maintainers:
  - github: CoelacanthusHex
    email: coelacanthus@archlinuxcn.org

build_prefix: extra-x86_64

pre_build_script: aur_pre_build(maintainers=['Auerhuhn'])

post_build: aur_post_build

update_on:
  - source: aur
    aur: packetry-git
  - source: alpm
    alpm: pango
    provided: libpango-1.0.so
  - source: alpm
    alpm: pango
    provided: libpangocairo-1.0.so
  - source: alpm
    alpm: cairo
    provided: libcairo.so
  - source: alpm
    alpm: gdk-pixbuf2
    provided: libgdk_pixbuf-2.0.so
  - source: alpm
    alpm: glib2
    provided: libgio-2.0.so
  - source: alpm
    alpm: glib2
    provided: libgobject-2.0.so
  - source: alpm
    alpm: glib2
    provided: libglib-2.0.so
  - source: alpm
    alpm: glib2
    provided: libgmodule-2.0.so
  - source: alpm
    alpm: gtk4
    provided: libgtk-4.so
  - source: alpm
    alpm: rust

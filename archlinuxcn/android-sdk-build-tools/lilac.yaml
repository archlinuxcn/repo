maintainers:
  - github: yan12125

build_prefix: multilib

pre_build_script: |
    aur_pre_build(maintainers="mynacol")

post_build_script: |
    aur_post_build()

update_on:
  - source: aur
    aur: android-sdk-build-tools

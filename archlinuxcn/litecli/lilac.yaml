maintainers:
  - github: masakichi

build_prefix: extra-x86_64

repo_depends:
  - python-cli_helpers

update_on:
  - aur: litecli
  - alias: python

pre_build: aur_pre_build
post_build: aur_post_build

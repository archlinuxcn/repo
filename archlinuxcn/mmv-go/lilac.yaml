maintainers:
  - github: wfxr

build_prefix: extra-x86_64

post_build_script: |
    git_pkgbuild_commit()
    update_aur_repo()

update_on:
  - source: github
    github: itchyny/mmv
    use_max_tag: true
    prefix: v

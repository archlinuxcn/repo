maintainers:
  - github: Kimiblock

pre_build_script: update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()
#  update_aur_repo()

update_on:
  - source: "git"
    git: "https://git.code.sf.net/p/ink/git"
    prefix: "v"
    include_regex: "v.*"
    exclude_regex: "version.*"
build_prefix: extra-x86_64

repo_depends:
  - libinklevel
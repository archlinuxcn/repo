pre_build_script: aur_pre_build(maintainers=['figue'])

post_build: aur_post_build

time_limit_hours: 4

repo_depends:
  - perl-file-rename

update_on:
  - source: aur
    aur: icecat

maintainers:
  - github: dctxmei

maintainers:
  - github: yan12125

build_prefix: extra-x86_64

repo_depends:
  - lxqt-build-tools-git
  - liblxqt-git

pre_build_script: |
    aur_pre_build(maintainers=['pmattern'])

post_build_script: |
    aur_post_build()

update_on:
  - source: github
    github: lxqt/lxqt-openssh-askpass
  - source: aur
    aur: lxqt-openssh-askpass-git

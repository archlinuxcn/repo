maintainers:
  - github: taotieren

build_prefix: extra-x86_64

repo_depends:
  - python-setuptools-git-ls-files

post_build_script: |
  git_pkgbuild_commit()

update_on:
  - source: pypi
    pypi: skia-pathops
  - alias: python

pre_build_script: update_pkgver_and_pkgrel(_G.newver.lstrip('v'))

post_build: git_pkgbuild_commit

update_on:
  - source: gitlab
    gitlab: utilities/isoimagewriter
    host: invent.kde.org
    use_max_tag: true

maintainers:
  - github: rocka

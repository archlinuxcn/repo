post_install() {
    echo
    echo 'This package requires dialog support. Without it, the "Create New Project" feature may malfunction.'
    echo 'Install kdialog (for KDE), zenity (for GNOME), or a similar tool based on your desktop environment.'
    echo
}

post_upgrade() {
    post_install
}

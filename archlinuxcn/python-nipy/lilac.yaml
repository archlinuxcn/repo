maintainers:
  - github: hubutui

build_prefix: extra-x86_64

repo_depends:
  - python-nibabel

update_on:
  - github: nipy/nipy
    use_max_tag: true
    include_regex: "[^rc]+"
  - alias: python
# vim:set ts=2 sw=2 et:


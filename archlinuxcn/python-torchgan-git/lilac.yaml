maintainers:
  - github: hubutui

build_prefix: extra-x86_64

pre_build: vcs_update

post_build: git_pkgbuild_commit

depends:
  - python-torchvision

update_on:
  - github: torchgan/torchgan
  - alias: python
# vim:set ts=2 sw=2 et:


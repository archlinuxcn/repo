#!/bin/bash

post_install() {
    /usr/bin/ramroot -CE
}

pre_upgrade() {
    if [[ "$2" =~ ^1 ]]; then
        /usr/bin/ramroot remove
    fi
}

post_upgrade() {
    if [[ "$2" =~ ^1 ]]; then
        /usr/bin/ramroot -CE
    fi
}

pre_remove() {
    /usr/bin/ramroot -D
}

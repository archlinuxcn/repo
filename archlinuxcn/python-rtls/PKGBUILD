# Maintainer: lilydjwg <lilydjwg@gmail.com>

pkgname=python-rtls
_pkgname="${pkgname/python-/}"
pkgver=2026.4.24
pkgrel=1
pkgdesc="A drop-in replacement for Python's ssl module, backed by rustls"
arch=('x86_64' 'x86_64_v3' 'aarch64')
url='https://github.com/jawah/rtls'
license=('MIT')
depends=(
  python-wassima
)
makedepends=(
  python-build
  python-maturin
  python-installer
  python-wheel
)
optdepends=(
  'python-orjson: for speedups'
)
source=("${pkgname}-${pkgver}.tar.gz::${url}/archive/refs/tags/${pkgver}.tar.gz")
sha256sums=('8a1cdea1040c477fa565d8251cc16e29f693dd37d1028a7821e68b72d4a2446a')

build() {
  cd "${_pkgname}-${pkgver}"

  # or aws_lc_* symbols are undefined
  export CFLAGS=${CFLAGS/-flto=auto/}
  python -m build --wheel --no-isolation
}

package() {
  cd "${_pkgname}-${pkgver}"

  python -m installer --destdir="${pkgdir}" dist/*.whl

  install -Dm0644 -t "${pkgdir}/usr/share/licenses/${pkgname}" LICENSE
}

#

maintainers:
  - github: yuyichao

build_prefix: extra-x86_64

pre_build: aur_pre_build

post_build: aur_post_build

repo_depends:
  - rocm-llvm

makepkg_args: ["--nocheck"]

update_on:
  - source: aur
    aur: hsakmt-roct
  - source: manual
    manual: 2

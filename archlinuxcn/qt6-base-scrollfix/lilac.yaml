maintainers:
  - github: heddxh

pre_build_script: update_pkgver_and_pkgrel(_G.newver.split("-", 1)[0])

post_build_script: git_pkgbuild_commit()

update_on:
  # update on qt6 rebuild but only pass stripped version.
  - source: archpkg
    archpkg: qt6-base

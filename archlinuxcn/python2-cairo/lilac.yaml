maintainers:
  - github: ykelvis

build_prefix: extra-x86_64

pre_build_script: aur_pre_build(maintainers=['Archange', 'hexd'])

post_build: aur_post_build

repo_depends:
  - python2

update_on:
  - source: aur
    aur: python2-cairo

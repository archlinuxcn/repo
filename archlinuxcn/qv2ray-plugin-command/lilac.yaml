pre_build_script: aur_pre_build(maintainers=['dctxmei'])

post_build: aur_post_build

repo_depends:
  - qv2ray

update_on:
  - source: aur
    aur: qv2ray-plugin-command

maintainers:
  - github: dctxmei

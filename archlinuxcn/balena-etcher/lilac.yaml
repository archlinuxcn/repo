repo_depends:
  - nvm
  - electron19-bin

maintainers:
  - github: zhullyb

pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)

post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()

update_on:
  - source: github
    github: balena-io/etcher
    use_latest_tag: true
    prefix: v

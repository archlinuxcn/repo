maintainers:
  - github: imlonghao
    email: imlonghao@archlinuxcn.org

build_prefix: extra-x86_64

pre_build: aur_pre_build(maintainers=['ogarcia'])

post_build: aur_post_build

update_on:
  - aur: telegraf

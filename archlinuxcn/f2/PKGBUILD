# Maintainer: aulonsal <seraur at aulonsal dot com>

pkgname=f2
pkgver=2.0.0
pkgrel=1
pkgdesc="A cross-platform command-line tool for batch renaming files and directories quickly and safely"
arch=('x86_64' 'aarch64')
url="https://github.com/ayoisaiah/${pkgname}"
license=('MIT')
depends=('glibc')
makedepends=(
    'go'
    'gotestfmt'
    'just'
    'perl-image-exiftool'
)
conflicts=("${pkgname}-bin")
source=("${pkgname}-${pkgver}.tar.gz::${url}/archive/v${pkgver}.tar.gz")
b2sums=('8ac38895256fdfbdd26c9e3383752400f49e208eea77cc8ad1511899aa785c5691efd6fd103867c6aa3e974f501c457bd27823f3abc25ec2eeefca7295dda4de')

build() {
    cd "${srcdir}/${pkgname}-${pkgver}"
    export CGO_CPPFLAGS="${CPPFLAGS}"
    export CGO_CFLAGS="${CFLAGS}"
    export CGO_CXXFLAGS="${CXXFLAGS}"
    export CGO_LDFLAGS="${LDFLAGS}"
    export GOFLAGS="-buildmode=pie -trimpath -ldflags=-linkmode=external -mod=readonly -modcacherw"
    just build
}

check() {
    cd "${srcdir}/${pkgname}-${pkgver}"
    just test
}

package() {
    cd "${srcdir}/${pkgname}-${pkgver}"
    install -Dm0644 LICENCE -t "${pkgdir}/usr/share/licenses/${pkgname}/"
    install -Dm0644 README.md -t "${pkgdir}/usr/share/doc/${pkgname}/"
    install -Dm0755 "bin/${pkgname}" -t "${pkgdir}/usr/bin/"
}

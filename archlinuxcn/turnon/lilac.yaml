maintainers:
  - github: Kimiblock

pre_build_script: update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()
  update_aur_repo()

update_on:
  - source: "gitea"
    gitea: "swsnr/turnon"
    host: "codeberg.org"
    use_max_tag: true

  - source: alpm
    alpm: libadwaita
    provided: libadwaita-1.so
    strip_release: true
#  - aur: turnon
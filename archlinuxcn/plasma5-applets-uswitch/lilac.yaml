pre_build_script: update_pkgver_and_pkgrel(_G.newver)

post_build: git_pkgbuild_commit

update_on:
  - source: gitlab
    gitlab: divinae/uswitch
    host: gitlab.com
    use_max_tag: true

maintainers:
  - github: rocka

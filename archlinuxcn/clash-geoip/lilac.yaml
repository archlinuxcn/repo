pre_build_script: |
    update_pkgver_and_pkgrel(_G.newver)

pre_build_script: |
    vcs_update()

post_build_script: |
    git_add_files('PKGBUILD')
    git_commit()
    update_aur_repo()

update_on:
  - source: github
    github: MetaCubeX/meta-rules-dat
    branch: release
  - source: manual
    manual: 1.0.1

maintainers:
  - github: dctxmei

maintainers:
  - github: Kimiblock

pre_build_script: |
  update_pkgrel()

  #  update_pkgver_and_pkgrel(_G.newver)

post_build_script: |
  git_pkgbuild_commit()
#  update_aur_repo()

update_on:
  - source: "alpm"
    alpm: "firefox"
    #strip_release: true
  - source: "alpm"
    alpm: "ffmpeg"
    provided: "libavcodec.so"
    strip_release: true

repo_depends:
  - portable-packer

build_prefix: extra-x86_64

maintainers:
    - github: poscat0x04

update_on:
    - source: aur
      aur: nodejs-spago
repo_depends:
    - purescript
    - ncurses5-compat-libs

pre_build: aur_pre_build

post_build: aur_post_build


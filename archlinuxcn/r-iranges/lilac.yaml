build_prefix: extra-x86_64
maintainers:
  - github: hubutui
repo_depends:
  - r-biocgenerics
  - r-s4vectors
update_on:
  - regex: IRanges_([\d._-]+).tar.gz
    source: regex
    url: https://bioconductor.org/packages/IRanges

_name=TatSu-LTS
pkgname=python-tatsu-lts
pkgver=5.13.2
pkgrel=1
pkgdesc="TatSu takes a grammar in a variation of EBNF as input, and outputs a memoizing PEG/Packrat parser in Python."
arch=(any)
url="https://github.com/dnicolodi/TatSu-LTS"
license=(BSD-4-Clause)
depends=('python' 'python-setuptools')
makedepends=('python-setuptools' 'python-build' 'python-installer')
optdepends=('python-colorama' 'python-rich')
conflicts=('python-tatsu')
source=("https://files.pythonhosted.org/packages/source/${_name::1}/${_name}/tatsu_lts-5.13.2.tar.gz")
sha256sums=('7204dbc3075fabecedd1ec1cbe04c5f20b159d7344f63280e580a21f3c14bf9f')

build() {
  cd "$srcdir/tatsu_lts-5.13.2"
  python -m build --wheel --no-isolation
}

package() {
  cd "$srcdir/tatsu_lts-5.13.2"
  python -m installer --destdir="$pkgdir" dist/*.whl

  # make sure we don't install any world-writable or root-readable-only files
  # we shouldn't need to fix ownership as we extract tarballs as a non-root user
  # https://github.com/pypa/setuptools/issues/1328
  # https://github.com/LonamiWebs/Telethon/issues/1605
  chmod u=rwX,go=rX -R "$pkgdir"
  # make sure we don't install annoying files
  local _site_packages=$(python -c "import site; print(site.getsitepackages()[0])")
  rm -rf "$pkgdir/$_site_packages/tests/"
}


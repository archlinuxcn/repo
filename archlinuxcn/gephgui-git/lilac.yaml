pre_build: aur_pre_build

post_build: aur_post_build

repo_depends:
    - geph-client-git

update_on:
    - source: github
      github: geph-official/gephgui
    -

      source: aur
      aur:
maintainers:
    - github: dctxmei

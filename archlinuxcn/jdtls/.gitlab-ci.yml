include:
  - project: languitar-aur-packages/gitlab-templates
    ref: main
    file: PKGBUILD.gitlab-ci.yml

variables:
  PKGNAME: jdtls
  OPT_DEPS_TEST: python

test:
  script:
    - jdtls --help | grep usage

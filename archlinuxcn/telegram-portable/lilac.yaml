maintainers:
  - github: Kimiblock

pre_build_script: update_pkgver_and_pkgrel(_G.newver)
post_build_script: |
  git_pkgbuild_commit()

update_on:
  - source: "alpm"
    alpm: "telegram-desktop-lily"
    repo: "archlinuxcn"
    from_pattern: '-.*'
    to_pattern: ''

repo_depends:
  - portable
  - telegram-desktop-lily

build_prefix: archlinuxcn-x86_64
